import React from "react";
import Button from "../../Atom/Button/button";

const SubmitButton = ({ onClick }) => {
  return <Button text="Enviar" onClick={onClick} />;
};

export default SubmitButton;
